export class Post extends BaseEntity {
  // extends BaseEntity so we can use easy commands such as Post.find()
  there is also another pattern you can do, you can set up repositories for like larger projects